CREATE TABLE uscrn_data (
    WBANNO INT,
    UTC_DATE DATE,
    UTC_TIME TIME,
    LST_DATE DATE,
    LST_TIME TIME,
    CRX_VN VARCHAR,
    LONGITUDE DECIMAL,
    LATITUDE DECIMAL,
    AIR_TEMPERATURE DECIMAL,
    PRECIPITATION DECIMAL,
    SOLAR_RADIATION DECIMAL,
    SR_FLAG VARCHAR(1),
    SURFACE_TEMPERATURE DECIMAL,
    ST_TYPE VARCHAR,
    ST_FLAG VARCHAR(1),
    RELATIVE_HUMIDITY DECIMAL,
    RH_FLAG VARCHAR(1),
    SOIL_MOISTURE_5 DECIMAL,
    SOIL_TEMPERATURE_5 DECIMAL,
    WETNESS DECIMAL,
    WET_FLAG VARCHAR(1),
    WIND_1_5 DECIMAL,
    WIND_FLAG  VARCHAR(1)
);
